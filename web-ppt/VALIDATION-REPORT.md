# CLASS:ON 웹 PPT 검증 보고 · 개정 8

검증일: 2026-09-20

## 브라우저·화면

- Brave/Chromium과 인앱 브라우저에서 `1920×1080`, `1366×768`을 각각 확인했다.
- 개정 5의 본편 9장과 당시 Q&A 10장은 모두 `slideScroll 1920×1080`으로 내부 넘침이 없었다.
- 두 해상도 모두 body가 화면 크기를 넘지 않았고, 캔버스는 하단 조작 바를 제외한 영역에 맞춰 각각 0.9444, 0.6556 배율로 축소됐다.
- 개정 5의 본편과 Q&A 전체 캡처를 육안 검수해 글자 잘림, 카드 겹침, 이미지 왜곡, 버튼 겹침이 없음을 확인했다.
- 개정 3에서 5번 슬라이드의 세 단계 카드와 시연 패널을 아래로 확장한 뒤 두 해상도에서 다시 시각 검수했다.
- 개정 4에서 7번 슬라이드의 버전 변화를 `CLASS:ON 1.0 + 우리 반의 경험 → CLASS:ON 1.1` 화살표 도식으로 바꾸고 두 해상도에서 다시 시각 검수했다.
- 개정 5에서 하단의 개요·노트 버튼과 `O`·`N` 단축키, 관련 패널 기능을 제거했다. Q&A는 결론 슬라이드의 버튼으로 진입한다.
- 개정 6에서 Q&A 9개 주제를 카테고리별 FAQ 40문항으로 재구성했다. 각 카드에 질문·즉답·상태·Q&A 은행 ID를 함께 표시하고, `21.7% / 0.499`로 잘못 전재됐던 스트레스 수치를 원문 기준 `25.3% / 0.532`로 바로잡았다.
- 개정 7에서 q9의 첫 질문을 `이 경제 모델이 실제로 돌아갈 근거가 있나?`로 바꾸고, 답변 첫머리에서 시뮬레이션 수행 사실과 핵심 결과를 공개하도록 순서를 조정했다.
- 개정 8에서 접근성·개인정보의 서로 다른 5개 질문을 2장으로 분리했다. 담임 운영의 단계도입 중복 질문, 경제 모델의 맥락 없는 취약점 질문, 웹 프로토타입의 구현범위 중복 질문은 삭제·통합해 FAQ를 37문항으로 정리했다.

## 개정 8 정적 검증

- 슬라이드 ID 20개가 모두 고유함: 통과
- 본편 9장, Q&A 11장, FAQ 카드 37개 확인: 통과
- 모든 `data-goto` 대상이 실제 슬라이드 ID와 연결됨: 통과
- `script.js` 구문 검사: 통과
- 이전 오기 `21.7`, `0.499` 및 혼동 상태문구 잔존 여부: 없음
- 이번 실행에서는 로컬 서버와 인앱 브라우저 접근 승인 검토가 시간초과되어, 개정 8 FAQ 화면의 1920×1080·1366×768 재렌더링 검수는 완료로 표시하지 않는다.

## 조작

- 좌우 방향키·Home·URL hash 복구: 통과
- 터치 스와이프 이벤트로 s1→s2 이동: 통과
- 전체화면 진입과 종료: 통과
- 본편 s9에서 다음 키가 Q&A로 자동 진입하지 않음: 통과

## 웹 데모와 백업

- 시연 1 버튼을 두 번 열어 동일 주소 `web-demo/dist/?mode=present`로 진입: 통과
- 시연 1의 확장 동선인 `웹 게시 작업 → 지갑 → 신청곡 → 자리배치`를 발표자 노트와 슬라이드에 일치시킴: 통과
- `web-demo/dist/?mode=present#seating`에서 “필요한 자리는 먼저 보장합니다” 문구 확인: 통과
- 시연 2 버튼이 `web-demo/dist/?mode=present#debug`로 진입: 통과
- 일반 링크가 `class-on-demo-url` 메타 설정을 반영: 통과
- 시연 2 백업 버튼이 `05-debug-1-0.png` 라이트박스를 여는 것 확인: 통과
- 기존 01~08 캡처 경로와 파일 존재 여부 확인: 통과

## 인쇄

- print media에서 조작 바가 숨겨지고 본편·Q&A 모든 슬라이드가 표시됨: 통과
- 인쇄 시 stage transform이 제거되고 1920×1080 페이지가 순차 배치됨: 통과

## 남은 외부 검증

- 개정 8 Q&A 11장을 `1920×1080`, `1366×768`에서 다시 열어 카드별 글자 잘림과 하단 영역 겹침을 확인해야 한다.
- 실제 GitHub Pages URL은 아직 정해지지 않아 온라인 주소, HTTPS 환경의 팝업 정책, 탭 재사용은 배포 후 확인해야 한다.
- 발표실 프로젝터의 색감·오버스캔과 실제 발표기기의 Pretendard CDN 접속 여부는 현장에서 확인해야 한다.
- 새 캡처를 추가할 경우 `SCREENSHOT-BRIEF.md` 기준으로 촬영하고 본편에 넣기 전 다시 2개 해상도로 검수한다.
